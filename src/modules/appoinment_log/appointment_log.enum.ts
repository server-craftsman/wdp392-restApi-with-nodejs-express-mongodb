export enum AppointmentLogTypeEnum {
    PENDING = 'pending',
    CONFIRMED = 'confirmed',
    SAMPLE_COLLECTED = 'sample_collected',
    TESTING = 'testing',
    COMPLETED = 'completed',
    CANCELLED = 'cancelled'
}

